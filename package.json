{
  "name": "shargan",
  "version": "0.1.0",
  "scripts": {
    "start": "npm run start --prefix client && npm run start --prefix server",
    "dev": "npm run dev --prefix client && npm run start --prefix server"
  },
  "dependencies": {
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
